export type CategoryDataTypes = {
	route: string,
	params: {
		screen: string
	}
}

export type TopicDataTypes = {
	route: string,
	params: {
		topic: string,
		topicTitle: string
	}
}

export type PublisherDataTypes = {
	route: string,
	params: {
		source: string,
		sourceTitle: string
	}
}